# Dependency directories
node_modules/

# Capacitor native projects — regenerated from capacitor.config.ts via
# `npx cap add android` / `npx cap add ios`. Kept out of git so the repo stays
# web-first; run the add command locally before building the native shells.
android/
ios/
.gradle/

# Build outputs (Vite/PWA)
dist/
dist-ssr/
.svelte-kit/
.next/
.nuxt/
out/
build/

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# IDEs and editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.patch

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Alternate Data Streams (Windows)
*:Zone.Identifier
*.Identifier